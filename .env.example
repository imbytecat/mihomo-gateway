# Proxmox API Configuration
# Copy this file to .env and fill in your values
# Then use: set -a && source .env && set +a (Linux/macOS)
# Or load with direnv / dotenv

# Proxmox API URL (required)
PKR_VAR_proxmox_api_url=https://your-proxmox-host:8006/api2/json

# API Token ID: user@realm!token-name (required)
PKR_VAR_proxmox_api_token_id=root@pam!packer

# API Token Secret (required)
PKR_VAR_proxmox_api_token_secret=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Proxmox node name (required)
PKR_VAR_proxmox_node=pve

# Debian ISO path on Proxmox storage (required)
# Format: storage:iso/filename.iso
PKR_VAR_proxmox_iso_file=local:iso/debian-12.8.0-amd64-netinst.iso

# SSH password for Packer provisioning (required)
# This is only used during build, not retained in template
PKR_VAR_ssh_password=packer

# Optional: Skip TLS verification (for self-signed certs)
# PKR_VAR_proxmox_skip_tls_verify=true

# Optional: Custom VM ID (default: 9000)
# PKR_VAR_vm_id=9000

# Optional: Custom storage pool (default: local-lvm)
# PKR_VAR_proxmox_storage_pool=local-lvm
